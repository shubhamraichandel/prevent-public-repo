{
  "name": "prevent-public-repo",
  "version": "1.0.1",
  "description": "An application that prevent all repos from turning into public repos",
  "main": "server.js",
  "scripts": {
    "test": "jest",
    "start": "node server.js",
    "sandbox": "cp .env.sandbox .env && nodemon server.js",
    "dev": "cp .env.dev .env && nodemon server.js"
  },
  "author": "Shubham Raichandel",
  "license": "ISC",
  "dependencies": {
    "@octokit/app": "^13.0.9",
    "@octokit/auth-app": "^4.0.6",
    "dotenv": "^16.0.3",
    "eventsource": "^2.0.2",
    "octokit": "^2.0.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "engines": {
    "node": "16.17.x",
    "npm": "8.15.x"
  }
}
