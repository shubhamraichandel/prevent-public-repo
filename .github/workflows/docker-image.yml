name: Docker Image CI
env:
  IMG_TAG: ${{github.sha}}

on: [workflow_dispatch]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag prevent-public:${{env.IMG_TAG}}
